export const TEST_CONFIG = {
  durationMinutes: 60,
  maxViolations: 3,
  passPercentage: 60,
  teacherDashboardPassword: "teacher2026",
  showReview: true,
  rules: [
    "Enter your real name and surname before starting the test.",
    "The website will try to open the test in fullscreen mode when allowed by the browser.",
    "Leaving the page, switching tabs, minimizing the browser, or exiting fullscreen counts as a warning.",
    "After 3 warnings, the test is submitted automatically.",
    "Your progress is temporarily saved on this device until the test ends."
  ]
};
